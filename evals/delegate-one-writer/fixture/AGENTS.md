# Evaluation repository instructions

- `TASK.md` is the task authority.
- Exactly one worker may write.
- Only `label.py` and `test_label.py` may be edited.
- Use Python's standard library only.
- Verification is `python3 -m unittest -v` from this repository root.
- Do not commit, push, install dependencies, or access the network.
